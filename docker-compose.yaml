services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: zeroaxiis-api

    restart: unless-stopped

    ports:
      - "8080:8080"

    env_file:
      - .env

    networks:
      - backend

networks:
  backend: